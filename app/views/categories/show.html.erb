<div class="contaier">
  <h1>TRANSACTIONS</h1>
  <%= @total %>
  <ul>
    <% @entities.each do |entity|%>
      <%= link_to entity.name %>
      <%= link_to entity.amount %>    
    <% end %>
  </ul>
  <%= link_to "Add New Transaction",  new_category_entities_path(@category) %>
  <%= link_to ">" %>
</div>
